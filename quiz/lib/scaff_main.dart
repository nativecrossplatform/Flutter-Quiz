import 'package:flutter/material.dart';
import 'package:quiz/main_container.dart';
class ScaffMain extends StatelessWidget
{
      const ScaffMain({super.key});
      @override
      Widget build(BuildContext context)
      {
          return MainContainer();
      }
}